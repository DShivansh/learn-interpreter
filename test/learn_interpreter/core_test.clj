(ns learn-interpreter.core-test
  (:require [clojure.test :refer :all]
            [learn-interpreter.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 1 1)))
  (testing "I am a failure"
    (is (= 'a 'a))))

